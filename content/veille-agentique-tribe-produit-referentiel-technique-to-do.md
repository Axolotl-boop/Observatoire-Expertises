# To Do — Espace documentaire & Intelligence Produit de la Tribe

*Mise à jour : 28 juin 2026 — resynchronisé sur SPECS **v5.1** [**REFONTE LEAN DU DIGEST — le digest est produit en UNE passe, sans annotateur, sans chips, sans cartes, sans couche de confiance ; corps `corps-generateur-digest-lean.txt` (supersede `corps-format-B-v3.txt`), template lean (supersede v3), action `Annoter_digest` + concat retirées du flux (`Ecrire_digest` écrit la sortie générateur directe). Validé 2 runs pinnés 2026-06 (gate vert : anti-fabrication 6/6, tags conservateurs, zéro résidu ``` après fix). Roadmap de la refonte = `To-Do-Digest.md`. ⚠️ TOUT item ci-dessous mentionnant l'annotateur, les chips, les cartes, `[[DASH_JSON]]` ou le « Chantier v3 » est SUPERSÉDÉ — conservé comme trace, ne pas l'actionner. Live restant : armer (dépiner `mois` + flux ON), pointer le site Graph sur la structure lean, relancer les 3 KR Owners (registres vides), durcir State of X (lot octobre).**] Rappel **v5.0** : [**FORMAT B v3 ARMÉ POUR LE 1ER JUILLET — annotateur v1.6 DÉPLOYÉ (+ fix regroupement carte Convictions : un ▸ par ID, pressions en sous-puces, un seul slot Décision), RUN RÉEL PINNÉ 2026-06 BOUT-EN-BOUT PASSÉ (gate vert, 6/6 digests v3 complets), Block D & Block R CONFIRMÉS EN EXÉCUTION RÉELLE** (emploi 6/6 ; registres pm/datapm/qa remplis, pmm/productai/productops repli propre sans ID fabriqué), **variables dépinées + flux armé** (Récurrence `2026-07-01T06:00:00Z`, ON). **Garde-fou source-absente CÂBLÉ B/C/D** (re-vérifié export frais — la `Fiche-Assembleur-Format-B` export-24-juin était périmée ; = déjà l'état v4.1). **Fait neuf : State of X RECÂBLÉ dans `matiereCommune` mais NON GARDÉ** (SPOF avant la boucle — durcissement lot octobre, risque juillet nul car fichier présent). **Backlog annotateur v1.7 (non urgent — le chip reste juste)** : sur-claim « multi-axes » (compte les sources au lieu des axes **internes** distincts) + faux-ami emploi (`croisement emploi+Seyos`). **Readiness** : 3 registres vides (pmm/productai/productops) → KR Owners à relancer. **Décision : armer tel quel.** Rappel **v4.9** : **CHANTIER FORMAT B v3 — (d) ANNOTATEUR v1.6 CONSTRUIT & VALIDÉ EN TEST (4 passes au volet) + (c) PROJECTION DES 4 CARTES FUSIONNÉE DEDANS (option B)** — déploiement **avant le 1er juillet** (validation run réel pinné = prérequis dur avant d'armer) ; 3 arbitrages : P1 chip dérivé de la **provenance** (⬤ ≥1 propriétaire croisé ou non · ◐ convergence externe seule ≥2 · ◯ source unique ; tag/chip orthogonaux ; invariant `[structurel]⟹⬤`), P2 chips **hors corps des blocs** (couche + cartes ; slots retirés de la Partie I), P3 carte Convictions = convictions **sous pression** du Bloc 2 (ID + énoncé repris du Bloc 2, slot Décision toujours `[ en attente ]`) ; v1.6 = 7 tâches (downgrade étendu Bloc 2/3/4 — règle « Blocs 1,3,4 → `tag:null` » supprimée ; provenance ; **chip** ; thème en **étiquette markdown** [consommateur = site] ; indice ; cycle maigre ; **projection 4 cartes**), **`[[DASH_JSON]]` retiré** ; option B = même action/concat, **seul le corps change** (recordId inchangé, filet inchangé) ; 2 leçons d'annotation gravées — **no-op tag** [faux `[X]→[X]` → contrôle « X≠Y » + exemple négatif ; faiblesse = chip ◯] et **faux-ami « Croisement emploi »** [emploi × son baromètre APEC/Seyos/Noé/LPC = faisceau, jamais croisement — incarnation R2] ; finding : le Bloc 2 doit porter l'**énoncé** de la conviction (pas que l'ID) ; thème réactivé pleinement ; reste **(e) site Graph** + déploiement AI Builder (re-injection token `digest` via Input→Text) + run réel pinné. Rappel **v4.8** : **(a) GÉNÉRATEUR DURCI + (b) FETCH REGISTRES DÉPLOYÉS & VALIDÉS sur run réel 2026-06** (« v3 maintenant », live pour le 1er juillet) ; leçons gravées : `base64ToString(body(...)?['$content'])` [`Get file content` = objet base64], discriminateur registre **insensible à la casse** [`toLower`, IDs `PM-01`], **un exemple d'ID concret dans un prompt = gabarit de fabrication** [→ règle copie-seule, zéro exemple, interdit des IDs hedgés], clé de jointure minuscules, re-parentage Block D `is successful + is skipped` ×2 ; pm `pm-01…05` bindés / pmm repli propre / emploi intact ; registres pm/qa/datapm peuplés, productai/productops/pmm au gabarit ; reste (c) cartes · (d) annotateur v1.6 · (e) site, gaté par (d). Rappel **v4.7** [**ANNOTATEUR v1.5 DÉPLOYÉ DANS AI BUILDER & VALIDÉ EN TEST (digest pm 2026-06) — le seul bloquant dur du 1er juillet est SOLDÉ** ; token vivant, `theme`/`tagOrigine`/garde no-op OK ; `theme` sous-appliqué constaté mais non corrigé [consommateur R1 supersédé v4.6, aucun impact aval]. Rappel **v4.6** : **FORMAT B v3** : 4 blocs (source/drill-down) + 4 cartes de restitution (surface) — Avant-vente · Contenus activables · Compétences×séniorité · Convictions (slot décision humain) ; **consultation = SITE GRAPH lisant les `.md` direct → pile dashboard `[[DASH_JSON]]` + indexeur + `Spec-decompte-deterministe` + `Contrat-dashboard-data` SUPERSÉDÉES, contenu markdown only** ; registre `Convictions-<exp>` déposé dans `04-Convictions-Expertises` (miroir humain lu-non-écrit) ; annotateur → v1.6 post-1er-juillet (retrait JSON)]. Rappel **v4.5** (**ASSEMBLEURS SNAPSHOT EMPLOI + CONCURRENTIEL EN RÉCURRENCE** : trigger `Recurrence` Month/3 Start `2026-10-01T03:00:00Z`, dates dynamiques [`utcNow() − 1 mois`, helper `moisQ1`], **garde « incomplet » concurrentielle posée** ; **arbitrage 1er run = 1er octobre / T3** [pas juillet : T2 verrouillé serait écrasé, le Format B lit le T2 existant jusqu'à sept.]. Reste avant le 1er run : dé-driver les libellés Teams [littéraux « T2 2026 » faux dès T3]). Rappel **v4.4** : run réel Format B 6/6 avec annotateur ; **annotateur v1.5 déployé dans AI Builder & validé en test (v4.7) → bloquant du 1er juillet soldé** [token `digest` via Input→Text, contrôle volet Test ; ⚠ marqueur `promptTokens` 45k+ vaut pour le générateur Format B, PAS l'annotateur (1 digest → ~4-5k normal)]. Rappel **v4.3/v4.2** : annotateur câblé, briefing publié sans gate, bloc autonome Format B livré (1er run 2026-07-01). Légende : ✅ fait · 🔄 en cours / partiel · ⏸️ parké · 🅿️ backlog post-V1 · ⬜ à faire · 🗑️ abandonné.*

> La source qui fait foi sur l'**état du système** reste les **SPECS** (v5.0). Cette To-Do liste les **tâches**.

> **Cadrage V1 (recadré v4.2).** Le **build du pipeline est terminé**, le **bloc autonome est livré** (Récurrence, 1er run 2026-07-01) — mais la **finalité a changé** : plus de gate humain, le digest est un **briefing publié**. Le chemin critique V1 n'est plus la validation KR Owner (dissoute). **⚡ v5.1 — REFONTE LEAN** : le digest est désormais produit en **une seule passe** (générateur seul), **sans annotateur, sans chips, sans cartes, sans couche de confiance** (tous archivés — voir `To-Do-Digest.md`). Il reste : (1) **armer** (dépiner `mois` + flux ON) ; (2) **pointer le site Graph** sur la structure lean ; (3) **relancer les 3 KR Owners** aux registres vides. Gouvernance DLP (✅ go ouvert) en parallèle.

---

## Phase 0 — Fondations (Axel + Delphine)

- ✅ Arborescence SharePoint cible créée (avec `Veille-concurrentielle` et `Veille-emploi`).
- ✅ Droits d'accès par dossier réglés — tout ouvert sauf `Pipe-Boond` brut.
- ✅ Dossier `00-Comment-ça-marche` rédigé.
- ✅ Template du digest par expertise figé. **v4.6 : Format B v3** — 4 blocs (source/drill-down) + 4 cartes de restitution (surface : Avant-vente · Contenus activables · Compétences×séniorité · Convictions) ; registre `Convictions-<exp>` ajouté (déposé dans `04-Convictions-Expertises`).
- ✅ **`Grille-validation-Format-B`** créée. **Durcie v4.0** (downgrade `[structurel]` newsletter/sponsorisé + faisceau ≠ corroborations). **Recadrée v4.2** : double emploi = **moteur de l'agent-annotateur** (downgrades + annotations de provenance) **+ literacy de lecture critique** pour le consommateur ; section « verdict » remplacée par « annotations émises ». ✅ **Déposée en canonique à la RACINE de `02-Digests-par-expertise/`** (source unique).
- 🔄 Désigner un responsable par brique. *(Axel porte concurrence, emploi, State of X, Boond, synthèse ; REX/expeditions et REX "capté" restent à attribuer — condition de survie.)*

## Phase 1 — Sources et captation (Delphine)

- ✅ Liste d'abonnements par expertise finalisée (11 existantes + ajouts).
- ⬜ S'abonner aux nouvelles sources PMM, QA, Product AI, Data PM (newsletters produit).
- ✅ Point de captation unique (dossier Outlook dédié) + flux Power Automate de routage.
- ✅ Prompt de pré-digestion Copilot (Format A) — gelé, gate `[[PAS_DE_FICHE]]`, sous-dossiers mensuels.
- ✅ Agent Format A testé/validé sur PM.

## Phase 2 — Briques propriétaires

- 🔄 Dépôt mensuel de l'extract Boond. *(récurrent, le fichier écrase le précédent — voulu.)*
- ✅ **Retraitement PAD — automatisé de bout en bout** (delta mensuel → agrégats → synthèse anonymisée ; JSON brut restreint dans `Pipe-Boond`).
- 🔄 **REX / learning expeditions** — automatisation ⏸️ parkée (poids négligeable + blocage droits Notion) ; 4 expeditions déposées en PDF. ⬜ Reste le régime "capté" (Forms → liste SharePoint) pour REX de mission + objections.
- ✅ **Rapports State of X** — index curé + PDF déposés *(+ Eleven Research Lab + Baromètre Produit Wivoo 2024-2025)*. Cadence annuelle.
- ✅ **Veille emploi — FINALISÉE.** Captation Cowork (couche recherche, 6 expertises, dépôt OneDrive synchronisé) ; snapshot brique pure v3 (agent hybride, salaire hors périmètre) ; **assembleur trimestriel validé bout-en-bout sur réel T2** (fan-out 6 expertises, `Do until` retry-jusqu'à-valide → 6/6 complets) ; baromètres groundés `Rapports-Emploi/` (APEC + Seyos/Noé/LPC) ; **spot-check des chiffres FAIT → gate convictions VERT** ; set T2 verrouillé (LPC entre à T3).

### Veille concurrentielle — état détaillé

- ✅ **Format refondu** : v2 (par axe) puis **v3.1** (brique pure : 2 lignes/axe `①②` + lecture transverse + tableau 3-états `✅/❌/⬜` avec ⬜≠❌ ; croisement ③ + positionnement ④ migrés au **Format B**). Périmètre **12 cabinets** (PM/QA + cohorte Data/IA).
- ✅ **Captation à deux étages câblée** :
  - 4 flux **RSS** ON (Thiga, Smartesting, Qestit, Converteo). ⬜ Bout-en-bout live au prochain billet réel ; ⬜ revérifier `fileName` durci au 1er billet Smartesting (trailing slash).
  - 1 flux **e-mail commun** (alertes + newsletters) ON, **chemin alerte validé** (Google Alert AI Builders → skip correct). ⬜ Valider le **chemin newsletter** (needle de domaine) au 1er mail ; ⬜ enrichir la règle Outlook avec les 5 domaines.
  - 12 **Google Alerts** qualifiées (alerte AI Builders re-qualifiée `"AI Builders" "Stéphane Roder"`).
- ✅ **Agent de pré-draft snapshot — concurrentiel-pur, créé, publié, VALIDÉ** (prompt v3.1).
- ✅ **Flux d'assemblage trimestriel — CONSTRUIT & VALIDÉ sur matière réelle** (run réel sur 4 fiches juin, 2 axes, dépôt `Veille-concurrentielle/2026-T2/`).
- ⬜ Snapshot concurrentiel **canonique-riche** à venir quand la captation aura accumulé.

## Phase 3 — Déploiement par expertise (Delphine puis squads)

- ✅ **`Assembleur Format B — mensuel` — CONSTRUIT & VALIDÉ 6/6** (3 min 06). Génération sur **action AI Builder GPT-4.1** (« Run a prompt », prompt `Pré-draft Format B` porté en invite) après le pivot bloc 5 — l'agent Copilot abandonné pour cette tâche (plafond < 10k). Pré-digest newsletters durci en amont ; `Do until`/sentinels/Switch/sortie validés. **DÉPINÉ & EN RÉCURRENCE (v4.1)** : `mois` dynamique, trimestre `-3 months`, trigger `Recurrence` Start time `2026-07-01T06:00:00Z`, flux réactivé.
- ✅ **RÉSOLU (v4.1) — incohérence `Recurrence` activée × `mois` pinné.** Le risque (chaque cycle réécrit juin + re-dépense AI Builder) est levé par le **dépinage** : `mois` est dynamique, le trigger pointe `2026-07-01T06:00:00Z` (premier run autonome sur juin), `trimestreCourant`/`trimLabel` sur `-3 months`. Le flux a été **désactivé le temps du chantier** puis **réactivé** une fois les 3 gardes en place — l'ordre correct (jamais réactiver avant d'avoir testé le chemin de dégradation).
- ✅ **Format B PM produit & propre** (`Format-B-pm-2026-06`). **Recadré v4.2 : il n'y a plus de "validation KR Owner" — le digest est un briefing publié.** Le `[structurel]` RICE/MoSCoW/MVP (porté par le signal Atlassian) est désormais **attrapé par l'agent-annotateur câblé v4.3** (downgrade `[structurel]→[tendance]`, motif corroboration interne absente). ✅ **`.md` annotés régénérés au run réel v4.4** (les 6 expertises, pin 2026-06/2026-T2 : couche de confiance + `[[DASH_JSON]]` sur chaque). Digest in situ dans `02-Digests-par-expertise/Product-Management/`.
- ✅ **Cadence actée** : mensuelle pour les 6 expertises ; veilles concurrence/emploi trimestrielles — **les deux snapshots en Récurrence depuis v4.5** (Month/3, 1er run autonome T3 le 1er octobre).
- ⬜ Handover aux squads / KR Owners — recadré v4.2 : **non plus "exercer la validation" mais "apprendre à miner"** le briefing (qu'en tirer pour decks/articles/skills) + lire la couche de confiance. Débloqué.

## Phase 4 — Appropriation & mesure (Axel / KR Owners)

- 🗑️ **`04-Convictions-et-offre` — DÉPRÉCIÉ (v4.2).** Plus de référentiel central : les convictions se forgent **au point d'usage** (decks d'expertise, articles). À rouvrir seulement si l'appropriation dispersée se révèle insuffisante.
- ⬜ Première synthèse Tribe mensuelle (`03-Synthèse-Tribe`).
- 🗑️ **Lier les REX inspirationnels depuis `04`** — sans objet (`04` déprécié) ; le réemploi REX se fait depuis les decks.
- ⬜ Suivi des 3 indicateurs (dont champ « source : veille » côté avant-vente) + **collecte des feedbacks consommateurs → backlog d'évolution** (nouveau, v4.2).
- ⬜ Première revue à 3 mois.

## Phase 5 — Annotation, consultation & continuité (Axel) — ⭐ CHEMIN CRITIQUE V1 (v4.2)

**Agent-annotateur (nouveau — chemin critique V1)**

- ✅ **Agent-annotateur CÂBLÉ (v4.3)** — post-passe AI Builder GPT-4.1 (invite `Prompt-agent-annotateur` **v1.4**) **dans le cas `digest` du Switch**, après génération avant `Ecrire_digest`, **filet** run-after-regardless (KO ⇒ digest brut). **Design couche-ajoutée-pas-réécriture** : émet **uniquement** la couche de confiance + le bloc `[[DASH_JSON]]` ; le flux concatène digest brut + couche. **Révision v4.3** : l'indice `confidenceSummary` passe de chiffré à **qualitatif** (un LLM ne recompte pas fiablement — décompte exact → déterministe au dashboard) ; `non corroboré` = label positif ; `tag: null` hors Bloc 2 ; `tagCorrige` ajouté. **Testé 4× en volet.** Annote, ne juge pas. ⬜ **Reste : le run réel PM bout-en-bout** (spot-check d'un 2ᵉ downgrade en variance run-to-run).
  - ⚠ **Trou résiduel gravé** : l'indice global rend lisible un digest pauvre, il **n'attrape pas un croisement fabriqué qui s'annonce solide** (option (b) échantillonnage humain écartée). Rouvrir (b) si un feedback en révèle un.
- 🔄 **Couche de confiance lisible non-expert** : **posée par l'annotateur (v4.3)** — provenance par signal (`prov`/`vendor`) + indice **qualitatif** `confidenceSummary` + `tagCorrige`. Côté **digest** : couche écrite en pied du `.md` ✅. Côté **dashboard** : affichage à câbler (tag + provenance + tuile indice + **décompte exact calculé en déterministe sur `prov`**).

**Dashboard de consultation — ⭐ PROMU SURFACE PRIMAIRE (v4.2)**

- ✅ **Prototype HTML autonome livré** (`dashboard-veille-tribe.html`).
- ✅ **Contrat d'interface produit** (`Contrat-dashboard-data.json`) — **recadré v4.2** : ajout `prov`/`vendor` (sortie annotateur) + `thinCycle` remplace `thresholdMet`/`reportSentence` (report abandonné).
- ✅ **Voie d'alimentation tranchée (Voie C)** : bloc JSON entre `[[DASH_JSON]]` / `[[/DASH_JSON]]` — **émis par l'agent-annotateur** (figé v4.2.1 : il porte l'état final annoté `prov`/`vendor`/`confidenceSummary`).
- 🔄 **`[[DASH_JSON]]` émis par l'annotateur (v4.3)** — validé conforme au contrat en **volet AI Builder** (4 itérations PM : tags, `prov` positifs, `vendor`, `tagCorrige`, JSON valide). ⬜ Reste : confirmer sur le **run réel PM** (chaîne complète, digest fraîchement généré) + câbler le **décompte exact déterministe** (dashboard, sur `sigs[].prov`).
- ⬜ **Câbler l'indexeur Power Automate** (deux passes) → `dashboard-data.json` en SharePoint, puis bascule du prototype sur `fetch('dashboard-data.json')`.
- ⬜ **Aligner `srcFor()`** du prototype sur le mapping bloc→catégories du contrat (§3.0).

**Référentiel technique**

- ✅ **Index + 16 fiches produits par crible verbatim** (exports `.zip`, invites Copilot/AI Builder, Office Script) — toute la chaîne de **production** documentée pour reprise/onboarding tiers.
- ⬜ **Compléter les reliquats `⟦À COMPLÉTER⟧`** (non bloquants) : texte d'invite **Pré-digest newsletters** + texte d'invite **`cr1c3_SynthsedemandeBoond`** (les 2 maillons décrits mais pas reproductibles à l'identique) ; **feedUrls** RSS Smartesting/Qestit ; confronter l'invite **live** `Pré-draft Format B` au fichier projet.
- ⬜ **Documenter le dashboard dans le référentiel** *(trou structurel : l'index se déclare complet en couvrant la production, mais la couche consultation n'a pas de fiche)* — `Fiche-Dashboard-consultation` + ligne d'index + couche dans le schéma de flux + ranger `Contrat-dashboard-data` dans l'inventaire.

## Phase 6 — Propagation documentaire (Axel)

- ✅ **Charte (`Description-du-projet`) v1.6 + guide (`00-Comment-ça-marche`) v1.5** — propagés au **recadrage v4.2** (briefing publié sans gate, agent-annotateur, rôle KR Owner valideur→consommateur, dashboard surface primaire, `04` déprécié, report abandonné). *(v1.5/v1.4 antérieures : 6 couches + pivot GPT-4.1 + grille intégrée.)*

---

## Bloquants V1 — RECADRÉS v4.2

1. 🗑️ **Validation KR Owner du Format B PM — DISSOUTE.** Plus de gate humain : le digest est un briefing publié, l'appropriation se fait au point d'usage. Remplacée par le **build de l'agent-annotateur** (Phase 5).
2. ✅ **Go/no-go DLP** — **go diffusion ouverte** tranché par la gouvernance data. Audience non bornée (cohérent avec l'ouverture directe à tous, v4.2).

## Dette technique ouverte (hors V1)

- ✅ **RÉSOLU (25 juin) — régression `matiere` (jeton de variable écrasé au recollage du prompt AI Builder)** : ré-injecté via Input→Text, run de bout en bout re-validé, leçon gravée dans `Fiche-Transverse-Patterns-Power-Automate` (§ AI Builder — prompts) + SPECS §9.A.bis.2. Marqueur de non-régression à l'avenir : `promptTokens` 45k+ au run (≈2 957 = injection tombée).
- ⬜ **NOUVEAU (v4.1) — label trimestre de la section « sources absentes » reconstruit par GPT-4.1, non fiable en dégradation.** Sur le run `2026-05`, le digest a écrit `SNAPSHOT EMPLOI pm T2-2026` là où le sentinel injecté disait `T1-2026` : le modèle **régénère** le label au lieu de le recopier et hallucine le trimestre canonique `T2` (omniprésent dans le grounding). **Cosmétique** : invisible en prod nominale (T2 sera juste quand c'est T2) ; la section garde sa valeur de signal (le KR Owner voit qu'il y a des trous). Fix éventuel = **émettre cette section par le flux en post-traitement** (donnée déterministe connue au niveau du flux) plutôt que de la confier au modèle. Faible levier de la corriger par prompt (contrainte décrite ne tient pas).
- 🗑️ **ABANDONNÉ (v4.2) — gate de seuil `[[REPORT_TRIMESTRIEL]]`.** Le report est supprimé : plus personne n'est forcé d'agir sur un mois creux → un cycle maigre se **déclare en clair** dans le digest (valeur diagnostique), il ne se défère pas. Le prompt et la sortie sont reframés en conséquence (toujours `[[FIN_DIGEST]]`). Le backlog « gate de seuil déterministe » tombe avec lui.
- ⬜ **Chemin newsletter** du flux e-mail à valider + règle Outlook + needles de domaine au 1er mail.
- ⬜ **Bout-en-bout live des 4 RSS** ; revérifier `fileName` Smartesting au 1er billet.
- ⬜ **Ménage one-shot** dossier juin (doublons + fiches vides pré-gate).
- ⬜ Nettoyage `U+2028`/`NBSP` dans `moteur-delta-boond` ; re-synthèse trimestrielle Boond ; supprimer fichiers de test `*-2026-06`.
- ⬜ **Conformité DLP** des flux à valider formellement *(cf. bloquant #2)*.
- ⬜ **Nit qualité** : corriger le décompte du périmètre (« 6 sur 10 » / « 14 cabinets » au lieu de 12) à la validation.
- ⚠️ **Libellés Teams hérités du clonage — RÉCURRENTS et désormais DRIFTANTS (promu cosmétique → à corriger avant le 1er run autonome snapshot).** Le ping `Post_a_message_to_myself` poste « Snapshot concurrentiel T2 2026… » (hérité du clone) sur le Format B (DM ce libellé faux chaque 1er du mois). **Étendu aux assembleurs snapshot** : avec les dates dynamiques (v4.5), tout littéral « T2 2026 » devient **faux dès T3** (1er octobre) — concurrentiel (vérifier ping succès), emploi (« Snapshot **concurrentiel** T2 2026 », double faute). Fix = interpoler `@{variables('libelleTrim')} @{variables('annee')}`. *(trouvaille v3.6/3.7, drift confirmé v4.5)*
- ✅ **Assembleur concurrentiel — garde « incomplet » POSÉE (v4.5)** : `Condition` après le `Do until` (même prédicat), branche False = dépôt « ⚠️ INCOMPLET » + cri Teams, ping succès dans la branche True. Parité emploi atteinte (et ping succès-dans-True légèrement mieux → micro-dette de backport vers l'emploi). Revue structurelle 6/6. *(trouvaille v3.7, soldée v4.5)*
- ⬜ **Ping d'incomplétude emploi — slug non interpolé** : la branche False écrit la chaîne littérale `snapshot <slug> incomplet` (le slug n'est pas résolu) → corriger l'expression. *(trouvaille v3.7)*
- ⬜ **Boond — auto-redéclenchement à confirmer** : `delta-boond-*.json` est écrit dans le dossier-trigger `Pipe-Boond` → vérifier qu'il n'y a pas de boucle (a priori auto-limité par le curseur de delta). *(trouvaille v3.7)*
- ⚠️ **NOUVEAU (v5.0) — State of X RECÂBLÉ mais NON GARDÉ (SPOF avant la boucle — lot octobre).** L'assembleur Format B a regagné un bloc State of X dans `matiereCommune` (`Lire_state_of_x` + `Empiler_state_of_x`, après la `Condition` concurrentiel) — **≠ « abandonné V1 »** (fiche/v4.6 périmés sur ce point). Mais `Empiler_state_of_x` est en runAfter `Succeeded` **seul** : si le fichier State of X est absent/renommé, `Lire_state_of_x` plante **sans filet** → la boucle ne démarre pas → **les 6 digests tombent** (même nature que le SPOF PAD soldé en v4.1, et avant la boucle = blast radius total). **Risque 1er juillet = nul** (fichier présent, cité partout dans les digests). **Fix (miroir Block C/D)** : catch `Failed/Skipped` sur `Lire_state_of_x` → `Empiler_state_of_x_indisponible` (sentinelle) → reconverger `Boucle_expertise` sur les deux. À grouper avec le lot durcissement octobre. *(trouvaille v5.0, export frais 28 juin)*

---

## Backlog post-V1 *(itération de l'outil — n'entre pas dans la V1)*

- ✅ **LIVRÉ (v4.1) — Bloc « passage en autonome ».** garde-fou source-absente (**trois** gardes : PAD + Block C + Block D, `Lire PAD` révélé au test comme 3ᵉ SPOF non anticipé) **+** dépinage (`mois` dynamique + trimestre `-3 months` + trigger **Récurrence** Start time `2026-07-01T06:00:00Z`) **+** arbitrage **trimestre courant-vs-dernier-complet TRANCHÉ = dernier complet** (forcé par la dispo des snapshots). Chemin de dégradation prouvé sur run réel. **Sort du backlog.**
- ✅ **LIVRÉ (v4.5) — les DEUX assembleurs snapshot (emploi + concurrentiel) en Récurrence.** Trigger `Recurrence` Month/3 Start `2026-10-01T03:00:00Z` ; 5 variables de trimestre dé-pinnées (calcul `utcNow() − 1 mois`, helper `moisQ1`) ; **garde « incomplet » concurrentielle posée** (prérequis : jamais de récurrence sans filet de dégradation). **Arbitrage 1er run = 1er octobre / T3, pas juillet** : T2 verrouillé serait écrasé ; le Format B lit le T2 existant jusqu'à septembre, ne réclame un T3 frais qu'au 1er octobre. Reste avant le 1er run autonome : **dé-driver les libellés Teams** (cf. dette ci-dessus).
- 🅿️ **(a) Unionner les `⚠` à la dédup du pré-digest — PREMIER item.** La dédup écrase les `⚠ poussé par <nom>` → un `[structurel candidat]`/`SIGNAL DOMINANT` sur convergence multi-cabinets ressort *propre* (échec invisible). Fix = invite du pré-digest (unionner les flags à la fusion). *Note v4.2 : le rattrapage aval passe désormais par l'**agent-annotateur** (qui pose `vendor`) plutôt que par un caveat de checklist humaine — mais le fix amont reste utile, l'annotateur ne peut pas restaurer un flag déjà blanchi en amont.* **⚠ CONFIRMÉ sur run réel (v4.4)** : sur les 6 digests 2026-06, `vendor` est **0/6 renseigné** alors que la prose nomme des sponsors (productai : Yeita/Dazl/Airtable/Claude/Miro/Screeb ; pm : Yeita/Dazl/Claude). Donc **deux trous** à fermer : (i) **amont** — unionner les `⚠` à la dédup du pré-digest ; (ii) **annotateur** — il ne porte pas les `⚠` de la prose au champ `vendor` structuré (fix prompt distinct, non fait — Axel a priorisé le décompte cette session).
- 🅿️ **(b/annotateur) annotateur v1.7 — corriger le sur-claim de croisement (CONFIRMÉ run réel v5.0, NON urgent — le chip reste juste).** Deux défauts de même cause racine, sur les 6 digests 2026-06 : (i) **« multi-axes » sur-attribué (~8×)** — l'annotateur compte le **nombre de sources** croisées au lieu des **axes internes distincts** ; le motif `(newsletters + concurrentiel + State of X)` est billé *multi-axes* alors qu'il n'a qu'**un seul interne** (concurrentiel) → devrait être *croisement sur 1 axe* ; (ii) **faux-ami emploi (1×)** — `croisement sur 1 axe (snapshot emploi + Seyos)` : emploi + son baromètre de grounding billé en croisement → devrait être `propriétaire : emploi · chiffre directionnel (Seyos)` (R2 / leçon v4.4 déjà gravée, à durcir au prompt). **Impact = triangulation apparente gonflée, PAS le chip ⬤** (correct partout — la confiance lue par le lecteur est juste). **Fix** = règle explicite « pour 1-axe vs multi-axes, COMPTE les sources **internes** distinctes (PAD/emploi/concurrence) ; le nombre d'externes n'entre jamais dans ce compte » + rappel emploi-faisceau. **Déploiement avec run de confirmation APRÈS le 1er juillet** (ne pas re-déployer à J-3 sur un gate vert).
- 🅿️ **Registres de convictions vides — pmm / productai / productops** : ces 3 `Convictions-<slug>.md` sont au gabarit (le pipeline dégrade en repli honnête, conviction en clair sans ID). **Action hors-pipeline** : relancer les KR Owners concernés pour les remplir (les IDs deviennent alors vivants au cycle suivant, comme pm/datapm/qa). *(constaté run réel v5.0)*
- 🅿️ **Injection-texte REX/State of X** dans la matière Format B (grounding PDF abandonné en V1). *Recadré v4.2.1 : l'ancien déclencheur conditionnel (« si le KR Owner juge trop maigre ») n'existe plus — **V1 sans REX, point** ; ne remonte du backlog que si les **feedbacks consommateurs** font émerger le besoin.* **MAJ v5.0 : State of X est désormais CÂBLÉ** (injection-texte dans `matiereCommune`, cf. dette « State of X non gardé ») — cet item ne concerne donc plus que **REX**.
- 🅿️ **Sous-emploi concurrentiel** : l'axe Data/IA n'est pas propagé vers `productai`/`datapm` au croisement.
- 🅿️ **Enrichir la veille concurrentielle — « méthode Antoine Simonian »** : récupération plus riche par **scraping LinkedIn** pour compléter le **faisceau d'analyse concurrentielle**. Branche en **surface ① « qui bouge »** du snapshot v3 (recrutement + prises de parole), **pas le croisement** (qui reste au Format B). ⚠️ **Arbitrage structurant à rouvrir** — tension avec « sources publiques / pas de scraping / LinkedIn = passe humaine (CGU) ». À trancher au dégel : (1) risque CGU / exposition cabinet, (2) branchement surface ① seulement, (3) augmente-t-il **ou remplace-t-il** la passe humaine. *Tant que non tranché par Axel : LinkedIn reste en passe humaine.* (cf. SPECS v2.2 §9.)

---

## Prochaine étape recommandée

Le pipeline est **autonome supervisé** et tourne en récurrence. **Recadrage v4.2** : la finalité a changé — le digest est un **briefing publié sans gate**, le chemin critique V1 n'est plus la validation mais le **build de l'annotation + de la consultation**. Par ordre de valeur :

1. ✅ **Agent-annotateur VALIDÉ SUR RUN RÉEL (v4.4)** — 6/6 digests 2026-06 annotés (couche + `[[DASH_JSON]]`), filet non déclenché, risque accepté **non matérialisé** (aucun croisement fabriqué). ✅ **Prompt v1.5 DÉPLOYÉ dans AI Builder & VALIDÉ en test (28 juin, v4.7)** (`theme`+`tagOrigine`+garde no-op ; token `digest` ré-inséré via Input→Text ; sortie annote les vrais signaux du digest pm 2026-06 = token vivant). **Bloquant du 1er juillet soldé.** Limite explicite inchangée : hygiène + provenance, pas garantie de fiabilité. *(Reste en calibration, non bloquant : `theme` sous-appliqué — sans impact, consommateur R1 supersédé v4.6 ; à re-checker la garde no-op sur datapm au 1er run.)*
2. **Couche de confiance lisible non-expert** : posée par l'annotateur ✅ côté digest (chips ⬤◐◯). ⟳ **v4.6 : l'affichage passe par le site Graph** (lit les `.md` direct) — le **dashboard `[[DASH_JSON]]` + `Spec-decompte-deterministe` sont caducs** ; les comptes éventuels se calculent côté site.
3. **Surveiller le 1er run autonome du 1er juillet** — 6 digests complets, **`promptTokens` 45k+ côté générateur Format B** (injection `matiere` ; ≠ annotateur qui ne porte qu'un digest), pas de sentinelle déclenchée par erreur, **la couche de confiance déposée sur les 6**, **et les champs `theme`/`tagOrigine` présents** (v1.5 déployé ✅) ; re-checker la **garde no-op sur datapm** (bug 2026-06).
4. 🗑️ **Dashboard `[[DASH_JSON]]` — SUPERSÉDÉ (v4.6)** : remplacé par le **site Graph** lisant les `.md` direct (contenu only). L'indexeur deux passes, `Spec-decompte-deterministe`, `Contrat-dashboard-data`, `dashboard-veille-tribe.html`, `dashboard-data.json` deviennent caducs — **à archiver, pas à câbler**.
5. **Handover aux KR Owners** — recadré : apprendre à **miner** le briefing (decks/articles/skills) + lire la confiance, plus à le valider. Collecter leurs **feedbacks → backlog**.
6. **Fix qualité (a)** — unionner les `⚠` à la dédup du pré-digest.
7. **Fix cosmétiques groupés** : libellé Teams résiduel ; label trimestre des « sources absentes ».

**Chantier v3 (activation des cartes — « v3 maintenant », live pour le 1er juillet)** : ✅ **(a) générateur v3 DÉPLOYÉ & VALIDÉ run réel 2026-06** — Partie I (4 blocs durcis : Bloc 1 « ce que l'externe ajoute » + « question pour creuser » + cabinets-par-axe ; Bloc 2 « conviction touchée » → ID registre `<slug>-NN` ; Bloc 3 socle/séniorité ; Bloc 4 « sur quoi ça tient »), **sans chips/légende/cartes** (aval) ; artefact `corps-format-B-v3.txt`. ✅ **(b) fetch registres CÂBLÉ & VALIDÉ** — **Block R** par expertise (jumeau Block D, en tête de boucle) lit `04-Convictions-Expertises/Convictions-<slug>.md`. **Leçons** : `base64ToString(body(...)?['$content'])` (`Get file content` = objet base64) ; discriminateur **insensible à la casse** (`toLower`, IDs `PM-01`) ; **règle copie-seule + zéro exemple d'ID** (un exemple concret = gabarit de fabrication → convictions inventées) ; clé normalisée minuscules ; re-parentage Block D `is successful + is skipped` sur les 2 branches registre. **pm copie ses vrais `pm-01…pm-05` bien bindés (zéro fabrication) ; pmm (gabarit) repli propre ; emploi intact.** Registres : pm/qa/datapm peuplés (vivants au 1er juillet), productai/productops/pmm au gabarit (repli). ⬜ **(e) site Graph pointé** — cartes en surface / blocs au déplier ; légende longue = composant site statique (non régénérée). **AVANT le 1er juillet** : déploiement AI Builder de v1.6 (coller `corps-annotateur-v1_6.txt` → re-injecter le token `digest` via **Input → Text**, jamais au clavier) **+ run réel pinné 2026-06** bout-en-bout (génération v3 → annotateur v1.6 → dépôt) avant d'armer. **Aucune brique amont à toucher.**
  - ✅ **(c) projection des 4 cartes** — **fusionnée dans l'annotateur (option B)**, pas de passe séparée.
  - ✅ **(d) annotateur v1.6** — construit (`corps-annotateur-v1_6.txt`), **VALIDÉ en test (4 passes au volet)** puis **DÉPLOYÉ dans AI Builder (v5.0)** : collage plein → re-injection token `digest` via Input→Text ; **+ fix regroupement carte Convictions** (un ▸ par ID, pressions multiples en sous-puces, un seul slot Décision — patché ×3). 7 tâches, chips dérivés de la provenance, projection des 4 cartes, `[[DASH_JSON]]` retiré ; tous garde-fous cardinaux OK. 2 leçons d'annotation gravées (no-op tag, faux-ami « Croisement emploi »). **Finding « énoncé au Bloc 2 » RÉSOLU sur run réel** (carte Convictions porte l'énoncé en clair) → pas de patch générateur. **Défaut résiduel → backlog v1.7** (cf. dette) : sur-claim « multi-axes » + faux-ami emploi (chip reste juste).
  - ✅ **RUN RÉEL PINNÉ 2026-06 BOUT-EN-BOUT PASSÉ (v5.0) — gate d'armement vert.** 6/6 digests v3 complets (Partie I + Légende + Couche + 4 cartes), zéro sentinel/`[[DASH_JSON]]`. **Block D re-parenté CONFIRMÉ** (emploi 6/6, ne saute plus). **Block R CONFIRMÉ** (pm/datapm/qa remplis → IDs+énoncés injectés ; pmm/productai/productops gabarit → repli propre sans ID fabriqué). Regroupement v1.6 tient en réel. Variables dépinées + flux armé `2026-07-01T06:00:00Z`.

---
*À faire évoluer après la première revue à 3 mois. Source d'état : SPECS v4.9.*
